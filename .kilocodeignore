# Kilocode ignore file — paths Kilo Code MUST NOT access

# ── Secrets and credentials ──────────────────────────────────────────────────
.env
.env.*
!.env.example
secrets/
credentials/
*.pem
*.key
*.pfx
*.p12
*.jks
*-key.*
*_key.*
private-key*
*-secret-key.*
*-api-key.*
api-key-*
secret-key-*
.mcp.json
.claude/settings/api_settings.json

# ── Build output and generated artifacts ─────────────────────────────────────
dist/
build/
*.egg-info/
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.ruff_cache/
.mypy_cache/
htmlcov/
coverage/
.coverage
node_modules/
.next/
.turbo/
*.tsbuildinfo

# ── Package manager lock files (regenerate, never edit) ──────────────────────
bun.lock
uv.lock
package-lock.json
yarn.lock

# ── Runtime and OS state ─────────────────────────────────────────────────────
.git/
.claude/
.omc/
*.log
*.log.*
*.pid
.DS_Store
Thumbs.db

# ── Binary and media assets ───────────────────────────────────────────────────
*.png
*.jpg
*.jpeg
*.gif
*.webp
*.pdf
*.zip
*.tar
*.gz
*.bz2
*.xz
*.7z
*.so
*.dylib
*.dll
*.class
*.jar

# ── Shell command restrictions ───────────────────────────────────────────────
# Commands that bypass read_file restrictions MUST NOT run against paths above:
# cat, head, tail, less, more, sed, awk, grep, rg, strings, xxd, od
