name: Lint

on:
  workflow_dispatch:

jobs:
  ruff:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v6
      - name: Validate agent configs
        uses: agent-sh/agnix@v0
        with:
          target: 'claude-code'
