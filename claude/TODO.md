add indexing command/agent that utilizes serena index, ast-grep, tree-sitter, repomix. use toon to output the index efficiently. look into sql/turso indexing

- https://www.npmjs.com/package/token-pilot


## claude auto-run

```bash
claude /plugin marketplace add jhlee0409/all-for-claudecode && claude /plugin install afc@all-for-claudecode
```
