# Module notes

Detailed module docs were consolidated to reduce token overhead.

Use:

- `../reference.md` for capture syntax and workflow
- `../examples.md` for common patterns
- `../rules/` for reusable scan rules
